[project]
name = "comfyui-imageir-promptengine"
description = "Read an image into IMAGE_IR with a vision model (llama.cpp, LM Studio, Gemini), then compose grounded prompts — including MiniMax H3 I2VA — that contain nothing the image did not show."
version = "2.0.0"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/ssain3d-lgtm/ComfyUI-ImageIR-PromptEngine"

[tool.comfy]
PublisherId = "ssain3d-lgtm"
DisplayName = "Image IR Prompt Engine"
Icon = ""
